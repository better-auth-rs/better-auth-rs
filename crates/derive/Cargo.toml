[package]
name = "better-auth-derive"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
description = "Derive macros for better-auth entity traits"
repository = { workspace = true }

[lib]
proc-macro = true

[features]
# Feature flags for conditional FromRow generation
# These must match the parent crate's feature names
sqlx-postgres = []
sqlx-sqlite = []

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }
